import { Router } from 'express';

const router = Router();

/**
 * @summary External (public) API routes
 * @description Routes accessible without authentication
 */

/**
 * @rule {be-api-organization-rules} External routes for public access
 * @remarks Placeholder for future public endpoints (health checks, public info, etc.)
 */

export default router;
