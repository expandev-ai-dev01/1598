import { Router } from 'express';

const router = Router();

/**
 * @summary Internal (authenticated) API routes
 * @description Routes requiring authentication and authorization
 */

/**
 * @rule {be-api-organization-rules} Internal routes for authenticated access
 * @remarks Feature-specific routes will be added here as features are implemented
 */

export default router;
